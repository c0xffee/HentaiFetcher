# System Patterns

## 🚨 TOOL USAGE RULES (AI 行為準則)
1. **Read-First**: Coding 前務必讀取 `activeContext.md`。
2. **Update-After**: 每次完成步驟後，更新 `implementation-plan.md` 的狀態。
3. **No-Silent-Changes**: 禁止擅自更改技術棧，必須先更新文件。
4. **Git-Atomic-Commit**: 每次完成一個原子修改 (Atomic Change) 後，必須自動執行 Git Commit。

## Architecture (系統架構)

```
┌─────────────────────────────────────────────────────────┐
│                    Discord Server                        │
│                         │                                │
│                    !dl <url>                             │
└─────────────────────────┬───────────────────────────────┘
                          │
                          ▼
┌─────────────────────────────────────────────────────────┐
│              HentaiFetcher (Docker Container)            │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────────┐  │
│  │ Discord Bot │──│ Download    │──│ PDF Converter   │  │
│  │ (discord.py)│  │ Queue       │  │ (img2pdf)       │  │
│  └─────────────┘  └──────┬──────┘  └────────┬────────┘  │
│                          │                   │          │
│                          ▼                   ▼          │
│                   ┌─────────────┐    ┌─────────────┐    │
│                   │ gallery-dl  │    │ Eagle       │    │
│                   │ (下載核心)   │    │ Metadata    │    │
│                   └─────────────┘    └─────────────┘    │
└─────────────────────────────────────────────────────────┘
                          │
                          ▼
┌─────────────────────────────────────────────────────────┐
│                   Synology NAS                           │
│  /volume1/docker/HentaiFetcher/downloads/               │
└─────────────────────────────────────────────────────────┘
```

## File Structure (檔案結構)
```
HentaiFetcher/
├── run.py              # 主程式 (Discord Bot + 下載邏輯)
├── Dockerfile          # Docker 映像定義
├── docker-compose.yml  # Docker Compose 設定
├── .env                # 環境變數 (DISCORD_TOKEN)
├── config/
│   ├── gallery-dl.conf # gallery-dl 設定
│   └── bot.log         # 日誌檔案
├── downloads/          # 最終輸出 (PDF + metadata)
├── temp/               # 暫存下載檔案
└── memory-bank/        # Vibe Coding 文件
```

## Git Workflow Standards
- **Feature Branch**: `feat/<feature-name>` (新功能開發專用)
- **Fix Branch**: `fix/<bug-name>` (修復專用)
- **Refactor Branch**: `refactor/<scope>` (重構)
- **Docs Branch**: `docs/<topic>` (文件更新)
- **Commit Message**: Conventional Commits
  - `feat: add user login form`
  - `fix: resolve api timeout`
  - `docs: update README`
  - `refactor: extract download logic`

## 🚫 RESTRICTED ACTIONS (絕對禁止事項)
1. **Direct Commit to Main**: 禁止直接在 `main` 分支進行 Commit。所有變更必須透過 PR 合併。
2. **Force Push**: 嚴禁在任何共享分支或主分支使用 Force Push。
3. **Agent Mode on Main**: 全自動 Agent 模式 (具備 Terminal 權限時) **嚴禁** 在 `main` 分支運行，必須切換至 Feature Branch。

## Error Recovery Strategy (錯誤恢復策略)
- **Sandbox Rollback**: 若在 Feature Branch 開發過程中遇到嚴重錯誤，允許使用 `git reset --hard HEAD~1` 回朔至上一個原子存檔點 (Save Point) 並重試。

## Testing Strategy (測試策略)
- **Unit Test**: [待規劃] 核心邏輯需有單元測試覆蓋
- **Integration Test**: [待規劃] Discord Bot 與 gallery-dl 互動測試
- **Manual Test**: 使用 `!ping` 驗證 Bot 連線

## Coding Standards (程式碼規範)
- **Language**: Python 3.9+
- **Style**: PEP 8
- **Docstring**: Google Style
- **Type Hints**: 建議使用
- **Logging**: 使用 `logging` 模組，不用 `print`
